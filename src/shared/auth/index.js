const isLoggedIn = require("./is-logged-in");
const hasRole = require("./has-role");
const isSuperAdmin = require("./isSuperAdmin");
const isAdmin = require("./is_adm");

module.exports = { isLoggedIn, hasRole, isAdmin, isSuperAdmin };
